[user]
	email = snow@bitdrift.io
	name = Snow Pettersen
[pull]
  rebase = false
[push]
  autoSetupRemote = yes
[color]
	ui = auto
[core]
  editor = nvim
[init]
	defaultBranch = main

[core]
  pager = delta
	excludesFile = /Users/snow/.global-gitignore

[interactive]
  diffFilter = delta --color-only

[delta]
  navigate = true    # use n and N to move between diff sections
  line-numbers = true
  syntax-theme = Nord

  # delta detects terminal colors automatically; set one of these to disable auto-detection
  # dark = true
  # light = true

[merge]
  conflictstyle = zdiff3
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
